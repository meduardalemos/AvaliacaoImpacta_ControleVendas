@model IEnumerable<ControleVendas.Models.Pedido>

@{
    ViewData["Title"] = "Index";
}

<div class="d-flex justify-content-between">
    <h2>Lista de pedidos</h2>
    <a asp-action="Create" class="btn btn-success">Cadastrar novo pedido</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produto_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendedor_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Produto_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vendedor_Id)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.PedidoID" class="btn btn-warning white-text btn-sm"> <i class="bi bi-pencil-fill"></i> Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.PedidoID" class="btn btn-primary white-text btn-sm"><i class="bi bi-info-circle-fill"></i> Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.PedidoID" class="btn btn-danger btn-sm"><i class="bi bi-trash-fill"></i> Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
