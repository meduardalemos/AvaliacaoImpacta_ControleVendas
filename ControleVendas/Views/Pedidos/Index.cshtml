@model IEnumerable<ControleVendas.Models.Pedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produto_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendedor_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Produto_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vendedor_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PedidoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PedidoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PedidoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
